apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnomad-elasticsearch.fullname" . }}
  labels:
    {{- include "gnomad-elasticsearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gnomad-elasticsearch.selectorLabels" . | nindent 4 }}
