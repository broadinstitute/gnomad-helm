apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gnomad-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "gnomad-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gnomad-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
