apiVersion: v1
kind: Service
metadata:
  name: {{ include "exome-results-browsers.fullname" . }}
  labels:
    {{- include "exome-results-browsers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exome-results-browsers.selectorLabels" . | nindent 4 }}
