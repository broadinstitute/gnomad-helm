apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnomad-browser.fullname" . }}
  labels:
    {{- include "gnomad-browser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gnomad-browser.selectorLabels" . | nindent 4 }}
