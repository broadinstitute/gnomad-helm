apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gnomad-browser.fullname" . }}-test-connection"
  labels:
    {{- include "gnomad-browser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gnomad-browser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
